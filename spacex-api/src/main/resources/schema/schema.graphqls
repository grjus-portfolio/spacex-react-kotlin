type Query {
    shows(titleFilter: String):[Show]
    launches(missionName:String):[Launch]
    launch(flightNumber:Int):Launch
    missions:[Mission]
    mission(missionId:String):Mission

}

type Show {
    title:String
    releaseYear:Int
}